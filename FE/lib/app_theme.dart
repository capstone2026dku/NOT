// lib/app_theme.dart
// lib/app_theme.dart
import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF005299);         // Dark Blue for main branding/buttons
  static const Color backgroundLight = Color(0xFFF8FAFC); // Light gray-blue background
  static const Color border = Color(0xFFE2E8F0);          // Divider and container border lines
  static const Color textDark = Color(0xFF0F172A);        // Main titles and headers
  static const Color textLight = Color(0xFF64748B);       // Secondary descriptions
  static const Color textMuted = Color(0xFF94A3B8);       // Unselected labels/hints
}